import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Product",
  description: "product in the shop",
};

const Products = () => {
  return <div>Products</div>;
};
export default Products;
